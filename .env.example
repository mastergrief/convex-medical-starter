# =============================================================================
# CONVEX MEDICAL STARTER - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# Convex (auto-configured by `npx convex dev`)
# -----------------------------------------------------------------------------
CONVEX_DEPLOYMENT=dev:your-deployment
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# -----------------------------------------------------------------------------
# Semble API Integration
# -----------------------------------------------------------------------------
# Get these from your Semble account settings
# API Docs: https://docs.semble.io/

# GraphQL API endpoint
SEMBLE_API_URL=https://api.semble.io/graphql

# Your Semble login credentials (used for API authentication)
SEMBLE_CLIENT_ID=your-semble-email@clinic.com
SEMBLE_CLIENT_SECRET=your-semble-password

# Webhook secret (generate a random string, configure in Semble webhook settings)
# Used to verify incoming webhook requests via HMAC-SHA256
SEMBLE_WEBHOOK_SECRET=your-webhook-secret-min-32-chars

# -----------------------------------------------------------------------------
# Optional: Feature Flags
# -----------------------------------------------------------------------------
# SEMBLE_SYNC_ENABLED=true
# SEMBLE_WEBHOOK_ENABLED=true
